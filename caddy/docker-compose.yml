version: "3.7"

networks:
  web:
    external: true
  discourse:
    external: true

services:
  caddy:
    image: ghcr.io/fariszr/caddy-dns:cloudflare-alpine@sha256:a7f9d1c6ef9b6290c8911d129ae8db87c095664c614901e90f3bb55c536fa02d
    container_name: caddy
    command: caddy run --watch --config /etc/caddy/Caddyfile --adapter caddyfile 
    cap_add:
      - NET_ADMIN
    restart: always
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    environment:
      - EMAIL=(email)
      - CLOUDFLARE_TOKEN=(cloudflare_token)
    configs:
      - source: Caddyfile
        target: /etc/caddy/Caddyfile
    volumes:
      - /home/aosus/caddy/configs:/etc/caddy/configs:ro
      - /home/aosus/caddy/proxy_error_page.html:/srv/proxy_error_page.html:ro
      - /home/aosus/gnulinuxsa-static:/srv/gnulinuxsa-static
      - data:/data
      - config:/config
    networks:
      web:
      discourse:

configs:
  Caddyfile:
    file: /home/aosus/caddy/Caddyfile

volumes:
  data:
  config: